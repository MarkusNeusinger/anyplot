library: highcharts
language: python
specification_id: heatmap-rainflow
created: '2026-03-02T21:06:18Z'
updated: '2026-06-02T10:04:29Z'
generated_by: claude-sonnet
workflow_run: 26811924655
issue: 4465
language_version: 3.13.13
library_version: unknown
preview_url_light: https://storage.googleapis.com/anyplot-images/plots/heatmap-rainflow/python/highcharts/plot-light.png
preview_url_dark: https://storage.googleapis.com/anyplot-images/plots/heatmap-rainflow/python/highcharts/plot-dark.png
preview_html_light: https://storage.googleapis.com/anyplot-images/plots/heatmap-rainflow/python/highcharts/plot-light.html
preview_html_dark: https://storage.googleapis.com/anyplot-images/plots/heatmap-rainflow/python/highcharts/plot-dark.html
quality_score: 88
review:
  strengths:
  - 'Perfect spec compliance: correct 2D heatmap with amplitude on Y-axis, mean on
    X-axis, logarithmic colorbar, and 20×20 bins all match the spec exactly'
  - 'Imprint sequential colormap correctly implemented (#009E73 green → teal → #4467A3
    blue) with custom color stops on a log color axis'
  - Theme-adaptive chrome is thorough — all text, backgrounds, annotation fill, and
    null color use the correct tokens for both light and dark themes
  - Annotation callout using Highcharts renderer.label() correctly identifies the
    peak low-amplitude, near-100-MPa-mean region
  - 'Zero-count bins shown as distinct null color (light #E0DECE / dark #2A2A26) which
    is visually clear in both renders'
  - 'Code quality is excellent: flat KISS structure, seed=42 reproducibility, all
    imports used, saves both .png and .html artifacts'
  - CDP setDeviceMetricsOverride + PIL pinning ensures exact 2400×2400 canvas dimensions
    — canvas gate passed
  - 'Library-idiomatic: correct container=''container'', HighchartsOptions.from_dict(),
    inline JS download for headless Chrome compatibility'
  weaknesses:
  - Subtitle font size (30px) is smaller than tick labels (44px), inverting the typographic
    hierarchy — subtitle should be ~48px to sit between axis labels (56px) and tick
    labels (44px)
  - The horizontal bell-curve pattern (dominant cycles near 100 MPa mean) is visually
    subtle — the logarithmic scale and wide Gaussian sigma (60 MPa) compress the horizontal
    contrast; consider narrowing sigma to ~40 MPa or using a linear color scale in
    addition to log to make the mean-stress pattern more immediately visible
  - DE-03 storytelling is primarily text-driven (subtitle + annotation) rather than
    visual — the heatmap color pattern alone does not strongly tell the 'near 100
    MPa mean stress dominates' story without reading the annotation
  - 'DE-02 refinement is good but the outer heatmap border could be removed (set plotBorderWidth:
    0) for a cleaner minimal look consistent with the style guide''s ''remove all
    spines for heatmaps'' recommendation'
  image_description: |-
    Light render (plot-light.png):
      Background: Warm off-white #FAF8F1 — correct, not pure white.
      Chrome: Title "heatmap-rainflow · python · highcharts · anyplot.ai" in dark #1A1A17 at 66px, fills ~80% of canvas width — readable and well-proportioned. Subtitle "Rainflow cycle counting — dominant cycles at low amplitude, near 100 MPa mean stress" in INK_SOFT at 30px — readable but slightly small relative to tick labels. Y-axis label "Cycle Amplitude (MPa)" at 56px and X-axis label "Cycle Mean (MPa)" at 56px — both clearly visible in dark text. Tick labels at 44px in INK_SOFT — all readable. X-axis tick labels rotated at -45° (315°), 20 values from -50 to 250 MPa, no overlap. Colorbar title "Cycle Count" at 44px, scale labels from 1 to 10k — fully readable.
      Data: The heatmap 20×20 grid shows a gradient from #009E73 green (low amplitude, high count) at the bottom to #4467A3 blue (high amplitude, low count) at the top. Subtle horizontal bell-curve pattern is visible — the bottom-center cells (mean ≈ 80-130 MPa) are slightly greener than the bottom-left and bottom-right corners. Two gray null-color patches (#E0DECE) at top-left (amplitude ~200 MPa, mean ~-50 MPa) and top-right (amplitude ~190 MPa, mean ~250 MPa) correctly mark zero-count bins. Annotation box in lower-right of heatmap with italic text identifying the peak region — readable.
      Legibility verdict: PASS — all text readable, no dark-on-light failures.

    Dark render (plot-dark.png):
      Background: Warm near-black #1A1A17 — correct, not pure black.
      Chrome: Title in light #F0EFE8 — clearly visible against dark background. Subtitle in INK_SOFT #B8B7B0 — readable. Y-axis and X-axis labels in light INK color — readable. Tick labels in INK_SOFT — readable. Colorbar labels in INK_SOFT on dark background — readable. No dark-on-dark failures detected — all text uses the correct light token for the dark theme.
      Data: Imprint sequential colormap colors (green → teal → blue) are identical to the light render — only chrome changed. Null-color patches now show as dark #2A2A26 against the near-black background — clearly distinct from the colored heatmap cells. Annotation box has dark fill (rgba(36,36,32,0.93)) with light italic text — readable.
      Legibility verdict: PASS — all text readable against dark background, no dark-on-dark failures.
  criteria_checklist:
    visual_quality:
      score: 28
      max: 30
      items:
      - id: VQ-01
        name: Text Legibility
        score: 7
        max: 8
        passed: true
        comment: All text readable in both themes. Subtitle at 30px is slightly below
          tick labels (44px) — minor hierarchy inversion. Annotation at 28px is small
          but readable as tertiary text.
      - id: VQ-02
        name: No Overlap
        score: 6
        max: 6
        passed: true
        comment: No text-on-text or text-on-data overlaps. Annotation box overlays
          heatmap cells intentionally.
      - id: VQ-03
        name: Element Visibility
        score: 5
        max: 6
        passed: true
        comment: Color gradient (green bottom, blue top) clearly visible. Horizontal
          bell-curve (mean direction) is subtle but present. Null-color bins clearly
          distinct in both themes.
      - id: VQ-04
        name: Color Accessibility
        score: 2
        max: 2
        passed: true
        comment: Imprint sequential (green→blue) is CVD-safe. No red-green as sole
          signal.
      - id: VQ-05
        name: Layout & Canvas
        score: 4
        max: 4
        passed: true
        comment: 'Canvas gate passed (2400×2400). Good proportions: generous margins,
          colorbar well-placed, title at ~80% width within expected range for mandated
          title length.'
      - id: VQ-06
        name: Axis Labels & Title
        score: 2
        max: 2
        passed: true
        comment: Y-axis 'Cycle Amplitude (MPa)', X-axis 'Cycle Mean (MPa)' — both
          descriptive with units. Title in correct format.
      - id: VQ-07
        name: Palette Compliance
        score: 2
        max: 2
        passed: true
        comment: 'imprint_seq colormap (#009E73 → #22828B → #4467A3) used for continuous
          data. Light bg #FAF8F1, dark bg #1A1A17. Both themes correct.'
    design_excellence:
      score: 11
      max: 20
      items:
      - id: DE-01
        name: Aesthetic Sophistication
        score: 5
        max: 8
        passed: true
        comment: 'Above defaults: custom imprint_seq colormap, annotation callout,
          subtitle, clean typography hierarchy. Subtitle size (30px) slightly smaller
          than tick labels is a minor flaw. Not exceptional but clearly above generic.'
      - id: DE-02
        name: Visual Refinement
        score: 3
        max: 6
        passed: true
        comment: gridLineWidth:0 removes heatmap grid. Invisible cell borders (borderColor=PAGE_BG).
          Tick marks removed (tickLength:0). Axis lines removed (lineWidth:0). Heatmap
          outer border still present — could be cleaner.
      - id: DE-03
        name: Data Storytelling
        score: 3
        max: 6
        passed: true
        comment: Subtitle + annotation add narrative context. But the horizontal bell-curve
          pattern in the mean direction is visually subtle — the story relies on reading
          text rather than seeing a dramatic color pattern.
    spec_compliance:
      score: 15
      max: 15
      items:
      - id: SC-01
        name: Plot Type
        score: 5
        max: 5
        passed: true
        comment: Correct 2D heatmap with amplitude on Y-axis and mean on X-axis.
      - id: SC-02
        name: Required Features
        score: 4
        max: 4
        passed: true
        comment: Sequential colormap (imprint_seq), logarithmic color scale, colorbar,
          axis labels with MPa units, tick labels at bin centers, zero-count bins
          visually distinct.
      - id: SC-03
        name: Data Mapping
        score: 3
        max: 3
        passed: true
        comment: Amplitude on Y-axis, mean on X-axis, count as color. 20×20 bins within
          spec range (10×10 to 64×64).
      - id: SC-04
        name: Title & Legend
        score: 3
        max: 3
        passed: true
        comment: Title is 'heatmap-rainflow · python · highcharts · anyplot.ai'. Colorbar
          titled 'Cycle Count'.
    data_quality:
      score: 15
      max: 15
      items:
      - id: DQ-01
        name: Feature Coverage
        score: 6
        max: 6
        passed: true
        comment: 'All spec aspects shown: 2D matrix, amplitude/mean axes, count-as-color,
          logarithmic scale, null bins, annotation.'
      - id: DQ-02
        name: Realistic Context
        score: 5
        max: 5
        passed: true
        comment: Amplitude 10-200 MPa, mean -50 to 250 MPa, counts 1-~10k — all physically
          plausible for fatigue analysis. Neutral engineering content.
      - id: DQ-03
        name: Appropriate Scale
        score: 4
        max: 4
        passed: true
        comment: MPa values appropriate for stress analysis. Logarithmic scale appropriate
          for wide count range. seed=42 for reproducibility.
    code_quality:
      score: 10
      max: 10
      items:
      - id: CQ-01
        name: KISS Structure
        score: 3
        max: 3
        passed: true
        comment: Flat sequential code, no functions or classes.
      - id: CQ-02
        name: Reproducibility
        score: 2
        max: 2
        passed: true
        comment: np.random.seed(42)
      - id: CQ-03
        name: Clean Imports
        score: 2
        max: 2
        passed: true
        comment: 'All imports are used: os, tempfile, time, urllib.request, pathlib,
          numpy, highcharts_core, PIL, selenium.'
      - id: CQ-04
        name: Code Elegance
        score: 2
        max: 2
        passed: true
        comment: No fake UI. Clean data generation and chart config. Appropriate complexity.
      - id: CQ-05
        name: Output & API
        score: 1
        max: 1
        passed: true
        comment: Saves plot-{THEME}.png and plot-{THEME}.html. Uses current Highcharts
          API.
    library_mastery:
      score: 9
      max: 10
      items:
      - id: LM-01
        name: Idiomatic Usage
        score: 5
        max: 5
        passed: true
        comment: Chart(container='container'), HighchartsOptions.from_dict(), inline
          JS download, CDP viewport override, PIL canvas pinning — all idiomatic per
          library guide.
      - id: LM-02
        name: Distinctive Features
        score: 4
        max: 5
        passed: true
        comment: colorAxis with log scale and custom stops, nullColor for zero bins,
          renderer.label() for annotation callout, custom tooltip with category label
          lookup, symbolHeight/symbolWidth for colorbar proportions.
  verdict: APPROVED
impl_tags:
  dependencies:
  - selenium
  - pillow
  techniques:
  - colorbar
  - annotations
  - html-export
  - hover-tooltips
  patterns:
  - data-generation
  - matrix-construction
  dataprep: []
  styling:
  - custom-colormap

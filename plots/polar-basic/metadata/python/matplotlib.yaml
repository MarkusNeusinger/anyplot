library: matplotlib
language: python
specification_id: polar-basic
created: '2025-12-23T19:42:39Z'
updated: '2026-07-24T23:47:01Z'
generated_by: claude-sonnet
workflow_run: 30133561866
issue: 800
language_version: 3.13.14
library_version: 3.11.1
preview_url_light: https://storage.googleapis.com/anyplot-images/plots/polar-basic/python/matplotlib/plot-light.png
preview_url_dark: https://storage.googleapis.com/anyplot-images/plots/polar-basic/python/matplotlib/plot-dark.png
preview_html_light: null
preview_html_dark: null
quality_score: 77
review:
  strengths:
  - Correct polar chart type with theta=hour-of-day, radius=temperature, matching
    the spec's directional/cyclical use case exactly
  - 'Follows the spec''s notes precisely: theta_zero_location(''N'') + theta_direction(-1)
    puts midnight at top and time flows clockwise, radial scale starts at 0 so data
    isn''t compressed near center'
  - 'Good data storytelling elements: dashed average-temperature reference ring plus
    a labeled peak annotation give the viewer an immediate anchor for interpreting
    the diurnal curve'
  - 'Careful detail work: radial tick labels get a background-colored bbox and an
    explicitly raised zorder so they stay legible over the filled data area instead
    of colliding with it'
  - 'Imprint palette and theme-adaptive chrome correctly applied — brand green #009E73
    identical in both renders, INK/INK_SOFT correctly threaded through title, ticks,
    grid, spine, and annotation box across both themes'
  - 'Clean KISS code structure: seeded reproducible data (np.random.seed(42)), only
    necessary imports, no functions/classes, correct plot-{THEME}.png output'
  weaknesses:
  - 'Title is truncated in BOTH renders: the mandated string ends ''...anyplot.ai''
    in the source (line 92) but the saved PNG only shows ''...anyplot.'' — the final
    ''ai'' is completely missing, with ~11px of clean background remaining before
    the physical canvas edge (verified by pixel-scanning both plot-light.png and plot-dark.png:
    last non-background pixel is at x=2389 of 2400, not at the border). This is not
    canvas-edge cropping, it looks like the long title (75 chars) at fontsize=12 is
    being clipped by an internal bbox/clip region before reaching the figure edge.
    Root cause: the title-length-scaling formula from prompts/library/matplotlib.md
    (''title_fontsize = max(8, round(12 * 67 / len(title)))'') was not applied — with
    len(title)=75 that formula gives ~11pt, and this is a 2400px-wide SQUARE canvas
    (not the 3200px landscape the formula''s baseline assumes), so an even smaller
    fontsize than 11pt is likely needed here. Fix: compute title_fontsize from the
    actual title length and reduce further to account for the narrower square canvas
    so ''anyplot.ai'' fully renders within bounds.'
  - Because of the above, the mandated title format is not fully displayed — this
    is a direct Spec Compliance (SC-04) miss even though the source string itself
    is correct.
  - 'Design Excellence is solid but not yet exceptional: the peak/average annotations
    are a good start but fairly standard (single peak callout + reference ring) rather
    than a distinctive, publication-level treatment — consider a bit more contrast/hierarchy
    (e.g., subtle color or size emphasis on the peak point itself) to lift Aesthetic
    Sophistication and Storytelling further.'
  - Peak temperature (23.4°C) occurs at 12 PM in the data model (sin curve peaks exactly
    at hour 12); most real diurnal cycles peak a couple hours after solar noon (~2–3
    PM) — a minor realism nit for Appropriate Scale, not a hard violation.
  image_description: |-
    Light render (plot-light.png):
      Background: Warm off-white, matches #FAF8F1 target, not pure white.
      Chrome: Title reads "Hourly Temperature Pattern · polar-basic · python · matplotlib" then "anyplot." — the trailing "ai" is missing entirely (confirmed via pixel scan: last non-background pixel at x=2389 of a 2400px-wide canvas, with clean background for the remaining ~11px before the edge, so this is an internal text-clip, not a canvas-border crop). Angular hour labels (12 AM, 3 AM, 6 AM, 9 AM, 12 PM, 3 PM, 6 PM, 9 PM) and radial °C tick labels (5°C–25°C) are all dark ink, clearly legible against the light background. Peak annotation box and avg-ring label are both legible.
      Data: Brand green (#009E73) fill, line, and scatter markers for the 24 hourly readings, forming a smooth diurnal curve with a clear trough near midnight and a peak near noon. Dashed gray-ink reference ring marks the daily average (14.8°C). All data points visible with white-edged markers, no overplotting given only 24 points.
      Legibility verdict: FAIL (title truncated — "ai" missing from the mandated "anyplot.ai" suffix in both themes).

    Dark render (plot-dark.png):
      Background: Warm near-black, matches #1A1A17 target, not pure black.
      Chrome: Same title truncation defect reproduces identically — pixel scan confirms the same clipping (text ends at x=2389/2400, background all the way to the edge). All other chrome (hour labels, °C tick labels, avg annotation, peak callout box) uses light ink (INK/INK_SOFT tokens) and is clearly readable against the dark background — no dark-on-dark failures found elsewhere.
      Data: Colors are identical to the light render — same #009E73 fill/line/markers, same dashed avg ring, same peak value and position. Only the chrome (background, text ink, grid, annotation box fill) flipped, as expected.
      Legibility verdict: FAIL (same title truncation as light render; everything else passes).
  criteria_checklist:
    visual_quality:
      score: 20
      max: 30
      items:
      - id: VQ-01
        name: Text Legibility
        score: 2
        max: 8
        passed: false
        comment: All present text is well-sized and legible, but the mandated title's
          trailing 'ai' is missing entirely in both renders — a hard legibility/completeness
          failure on the single most important text element.
      - id: VQ-02
        name: No Overlap
        score: 6
        max: 6
        passed: true
        comment: No collisions — radial tick labels use background boxes and a raised
          zorder to stay clear of the filled data area; annotations don't collide
          with data or each other.
      - id: VQ-03
        name: Element Visibility
        score: 6
        max: 6
        passed: true
        comment: 24 sparse points with s=90 markers, 2.5pt line, appropriately sized
          for this density — nothing barely visible.
      - id: VQ-04
        name: Color Accessibility
        score: 2
        max: 2
        passed: true
        comment: Good contrast throughout, single-hue series with alpha-adjusted fill,
          no red-green reliance.
      - id: VQ-05
        name: Layout & Canvas
        score: 0
        max: 4
        passed: false
        comment: Title content is cut off before reaching the canvas edge (missing
          'ai') — falls under 'content cut-off' in the 0-point tier even though it
          isn't a canvas-border AR-09 crop.
      - id: VQ-06
        name: Axis Labels & Title
        score: 2
        max: 2
        passed: true
        comment: Radial axis has a °C unit formatter, angular axis uses clear hour-of-day
          labels.
      - id: VQ-07
        name: Palette Compliance
        score: 2
        max: 2
        passed: true
        comment: 'First series is #009E73 in both renders, backgrounds and chrome
          are theme-correct (#FAF8F1 / #1A1A17), identical data color across themes.'
    design_excellence:
      score: 14
      max: 20
      items:
      - id: DE-01
        name: Aesthetic Sophistication
        score: 5
        max: 8
        passed: false
        comment: Above a plain default (filled area, edge-highlighted markers, formatted
          radial ticks) but not yet publication-tier polish; the broken title undercuts
          overall sophistication.
      - id: DE-02
        name: Visual Refinement
        score: 5
        max: 6
        passed: true
        comment: Subtle grid (alpha 0.15), background boxes behind radial ticks to
          avoid collision, generous whitespace around the polar frame.
      - id: DE-03
        name: Data Storytelling
        score: 4
        max: 6
        passed: false
        comment: Peak callout + average reference ring create real visual hierarchy
          and a clear takeaway, though it's a fairly standard treatment rather than
          an especially inventive one.
    spec_compliance:
      score: 13
      max: 15
      items:
      - id: SC-01
        name: Plot Type
        score: 5
        max: 5
        passed: true
        comment: Correct polar chart, theta/radius mapping matches the spec exactly.
      - id: SC-02
        name: Required Features
        score: 4
        max: 4
        passed: true
        comment: Visible-not-overwhelming radial gridlines, standard-interval angular
          labels, top-start clockwise time direction, radial scale not compressed
          near center — all spec notes satisfied.
      - id: SC-03
        name: Data Mapping
        score: 3
        max: 3
        passed: true
        comment: theta=hour, radius=temperature correct, all 24 points shown, loop
          correctly closed.
      - id: SC-04
        name: Title & Legend
        score: 1
        max: 3
        passed: false
        comment: Source title string is the correct mandated format, but the rendered
          title is truncated ('anyplot.' instead of 'anyplot.ai') in both themes —
          the displayed title does not match the required format. Legend correctly
          omitted for single-series.
    data_quality:
      score: 13
      max: 15
      items:
      - id: DQ-01
        name: Feature Coverage
        score: 5
        max: 6
        passed: true
        comment: Shows the full diurnal rise/peak/fall/trough cycle with realistic
          noise; captures the plot type's main characteristics well.
      - id: DQ-02
        name: Realistic Context
        score: 5
        max: 5
        passed: true
        comment: Hourly outdoor temperature is a real, neutral, comprehensible scenario.
      - id: DQ-03
        name: Appropriate Scale
        score: 3
        max: 4
        passed: true
        comment: Plausible temperature range and noise level; peak occurring exactly
          at 12 PM (vs. the more typical ~2-3 PM afternoon lag) is a minor realism
          nit.
    code_quality:
      score: 10
      max: 10
      items:
      - id: CQ-01
        name: KISS Structure
        score: 3
        max: 3
        passed: true
        comment: Linear imports -> data -> plot -> save, no functions/classes.
      - id: CQ-02
        name: Reproducibility
        score: 2
        max: 2
        passed: true
        comment: np.random.seed(42) set before noise generation.
      - id: CQ-03
        name: Clean Imports
        score: 2
        max: 2
        passed: true
        comment: os, numpy, matplotlib.pyplot, FuncFormatter — all used.
      - id: CQ-04
        name: Code Elegance
        score: 2
        max: 2
        passed: true
        comment: Clean and appropriately complex, including a well-commented zorder
          workaround for the radial ticks; no fake UI.
      - id: CQ-05
        name: Output & API
        score: 1
        max: 1
        passed: true
        comment: Saves plot-{THEME}.png, no deprecated calls, no bbox_inches='tight'.
    library_mastery:
      score: 7
      max: 10
      items:
      - id: LM-01
        name: Idiomatic Usage
        score: 4
        max: 5
        passed: true
        comment: Correct Axes-method usage throughout (fill_between, plot, scatter,
          annotate) and proper polar-specific configuration.
      - id: LM-02
        name: Distinctive Features
        score: 3
        max: 5
        passed: true
        comment: Uses set_theta_zero_location/set_theta_direction/set_rlabel_position
          — genuinely polar-specific API, though a fairly standard combination for
          this chart type.
  verdict: REJECTED
impl_tags:
  dependencies: []
  techniques:
  - polar-projection
  - annotations
  - manual-ticks
  patterns:
  - data-generation
  - explicit-figure
  dataprep: []
  styling:
  - alpha-blending
  - edge-highlighting
  - grid-styling
